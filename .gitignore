# Rust
/target/
/target-local/
**/*.rs.bk
# Cargo.lock IS tracked — binary crate, needed for reproducible
# Docker image builds and locked CI runs. See STANDARDS.md §2.

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Testing
coverage/

# Build artifacts
build/

# mdBook output
/book/book/

# sqlx offline artifacts (generated by `cargo sqlx prepare`)
# Committed when we add DB compile-time checks; ignored until then.
.sqlx/

# Claude Code working state
/.claude/

# Environment
.env
.env.local

# In-house development artefacts — cross-project refacto ruleset
# outputs (coverage matrix, divergence log, migration prose,
# operator porting checklist, boot-diagnostic captures, deviation
# log, and the accompanying refacto-specific backlog task). Kept
# on-disk during development; not published.
/DIVERGENCES.md
/REFACTO-DEVIATIONS.md
/docs/refacto/
/tasks/backlog/008-cross-impl-harness.md

# Maintainer handoff — internal-only. Carries verification
# details (container digests, live-sweep transcripts, the
# h2ck.me pipeline mechanics) that don't belong in a public tree.
# Kept on-disk during development; not published.
/HANDOFF.md

# mdBook linkcheck cache
/book/linkcheck/
