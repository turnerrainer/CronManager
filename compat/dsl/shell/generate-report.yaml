# Report generation
generate_report:
  trigger: "0 0 9 ? * MON"  # Every Monday at 9 AM
  type: exec
  command: ./scripts/samples/generate_report.sh
  allowedEnvs:
    - REPORT_TYPE
    - EMAIL_RECIPIENTS
