# POST request example with retry
trigger_webhook:
  trigger: "0 0 * * * ?"  # Every hour at minute 0
  type: http
  method: POST
  url: https://httpbin.org/post
  retryCount: 3
  retryDelay: 2000
  ignoreFailures: true
