# Simple GET request — health check with retry.
health_check:
  trigger: "0 */5 * * * ?"
  type: http
  method: GET
  url: https://httpbin.org/status/200
  retryCount: 3
  retryDelay: 1000
  ignoreFailures: true
